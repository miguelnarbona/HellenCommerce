version: "3.9"

services:
  osrm_cuba:
    image: osrm/osrm-backend
    container_name: osrm_cuba
    command: osrm-routed /data/cuba.osrm
    volumes:
      - /mnt/c/HellenData/osrm/cuba:/data
    networks:
      - hellen_network

networks:
  hellen_network:
    external: true

